<div class="container">
  <%= simple_form_for(@restaurant) do |r| %>
    <%= r.input :name %>
    <%= r.input :address %>
    <%= r.input :phone_number %>
    <%= r.submit "modify a restaurant", class: "btn btn-primary" %>
    <% end %>
</div>
